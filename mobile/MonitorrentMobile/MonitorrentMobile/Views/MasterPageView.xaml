<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Caliburn.Micro.Xamarin.Forms;assembly=Caliburn.Micro.Platform.Xamarin.Forms"
             x:Class="MonitorrentMobile.Views.MasterPageView" DetailPage="{Binding Detail}">
  <MasterDetailPage.Master>
    <ContentPage Title="Monitorrent" Icon="ic_menu_black_24dp.png">
      <StackLayout VerticalOptions="FillAndExpand">
                <ListView x:Name="PagesList" ItemsSource="{Binding Pages}" 
                  VerticalOptions="FillAndExpand" 
                  SeparatorVisibility="None"
                  ItemSelected="PagesList_OnItemSelected"
                  >
                    <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding Title}"/>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </StackLayout>
    </ContentPage>
  </MasterDetailPage.Master>
  <MasterDetailPage.Detail>
    <ContentPage x:Name="NavigationPage" />
  </MasterDetailPage.Detail>
  
</MasterDetailPage>